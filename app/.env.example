# URL du serveur llama-server local (cf. /llm/README.md). Optionnel : si le
# serveur n'est pas joignable, l'app retombe automatiquement sur des réponses
# générées par les règles (app/src/llm/fallbackResponder.js).
VITE_LLM_URL=http://127.0.0.1:8090
